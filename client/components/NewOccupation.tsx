{
  /*
// TODO
import { useNavigate } from 'react-router-dom'
import EditEventForm from './EditContactForm.tsx'
import { ContactData } from '../../models/Contact.ts'
//import LineupNav from './LineupNav.tsx'
import { useCreateContact } from '../apis/api.ts'

export default function NewContact() {
  const createEvent = useCreateContact()
  const navigate = useNavigate()
  const handleSubmit = async (data: ContactData) => {
    await createEvent.mutateAsync(data)
    navigate(`/occupations/add/new`)
  }

  return (
    <>
      <LineupNav />
      <h2>New Event</h2>
      <EditEventForm
        submitLabel="Create event"
        name=""
        day="friday"
        time=""
        locationId={1}
        description=""
        onSubmit={handleSubmit}
      />
    </>
  )
}
*/
}
